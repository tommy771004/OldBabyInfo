# 01 — Restore executable Part data tests

**What to build:** Restore the repository's Part data-test entry point so the existing schema and repository suites, plus future phstudy parity contract tests, are collected and executed instead of failing during shared test setup.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [x] Running the existing Part schema and repository test files collects their tests and reports their real results.
- [x] The smallest test-bootstrap or dependency correction keeps existing UI test support intact.
- [x] Typechecking and the full test suite complete successfully.
