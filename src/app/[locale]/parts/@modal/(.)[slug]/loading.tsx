import { DetailDialogShell } from "@/components/detail-dialog.tsx";

/** The card appears on the click; the record fills it when it arrives. */
export default function Loading() {
  return <DetailDialogShell />;
}
