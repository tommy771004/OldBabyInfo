/**
 * What the dialog slot shows when nothing was intercepted: a hard load of
 * `/parts`, of a detail page, of anything under `/parts` at all. Nothing.
 */
export default function ModalDefault() {
  return null;
}
